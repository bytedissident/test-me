import SwiftUI

struct ContentView: View {
    var body: some View {
        Button(action: {} , label: {
            Text("CTA")
        })
        Text("Hello, world!")
            .padding()
    }
}
